apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "headscale-admin.fullname" . }}-test-connection"
  labels:
    {{- include "headscale-admin.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "headscale-admin.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
